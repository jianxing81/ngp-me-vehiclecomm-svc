package com.cdg.pmg.ngp.me.vehiclecomm.techframework.messaging.enums;

/** Enum class for supported Endian formats. */
public enum BytesOrder {

  /**
   * Big Endian format.
   * <!-- -->
   * Most-significant byte is stored first (index 0).
   */
  BIG_ENDIAN,

  /**
   * Little Endian format.
   * <!-- -->
   * Least-significant byte is stored first (index 0).
   */
  LITTLE_ENDIAN
}
